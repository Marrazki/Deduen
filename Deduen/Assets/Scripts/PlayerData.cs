using UnityEngine;

public class PlayerData : MonoBehaviour
{
    public int vida, lvl, xp;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        vida = 10;
        lvl = 1;
        xp = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
