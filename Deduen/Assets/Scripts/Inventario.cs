using UnityEngine;

public class Inventario : MonoBehaviour
{
    public Item[] inventario = new Item[9];
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
